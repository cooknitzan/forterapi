const COIN_URI: string = "https://min-api.cryptocompare.com/data/pricemulti?";

const CONVERT_TO: string = "tsyms=";

const CONVERT_FROM: string = "fsyms=";

const SERVER_PORT: string = "5000";

export { COIN_URI, CONVERT_TO, CONVERT_FROM, SERVER_PORT };
